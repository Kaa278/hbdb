services:
  db-manager:
    build: 
      context: .
      network: host
    image: hbdb-main
    container_name: hbdb-manager
    restart: always
    ports:
      - "3000:3000"
    dns:
      - 8.8.8.8
      - 8.8.4.4
    env_file:
      - .env
